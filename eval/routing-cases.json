{
  "comment": "Canonical (prompt → expected discretionary skills) routing cases. Mirrors the intent of template/.ruler/tests/simulate-prompts.sh but is scored by a live model instead of keyword overlap. Expected lists name DISCRETIONARY skills only — P3.4 force-fire skills (tdd-workflow, repo-conventions, design-review, …) may appear in model output and are never penalized.",
  "cases": [
    { "id": "feat-route", "prompt": "Add a new protected route that fetches user profile data and shows a loading state", "expected": ["react-routing", "react-data-fetching", "react-state-management"] },
    { "id": "feat-form", "prompt": "Add a new signup form with email validation and async submit error handling", "expected": ["react-forms", "async-error-handling"] },
    { "id": "perf-rerender", "prompt": "Investigate why this component rerenders on every keystroke and reduce the rerender cost", "expected": ["react-performance", "react-render-optimization"] },
    { "id": "perf-bundle", "prompt": "Add a new dependency for date formatting and check the bundle size impact", "expected": ["bundle-size"] },
    { "id": "a11y-dialog", "prompt": "Add a confirmation dialog with keyboard navigation and focus management", "expected": ["accessibility"] },
    { "id": "sec-xss", "prompt": "Render user-submitted markdown safely without enabling raw HTML injection", "expected": ["frontend-security"] },
    { "id": "sec-tokens", "prompt": "Review how the auth token is stored and whether localStorage exposure is acceptable", "expected": ["frontend-security"] },
    { "id": "route-guard", "prompt": "Add an admin-only route that redirects unauthenticated users to /login", "expected": ["react-routing", "frontend-security"] },
    { "id": "test-component", "prompt": "Write a Vitest test for this component using Testing Library queries", "expected": ["react-testing", "vitest"] },
    { "id": "test-e2e", "prompt": "Write a Playwright test that walks the login flow including OAuth redirect handling", "expected": ["playwright-best-practices"] },
    { "id": "multi-statement-db", "prompt": "Implement order checkout: insert the order row and decrement stock across two tables atomically", "expected": ["database-transactions", "db-write-protocol"] },
    { "id": "single-write", "prompt": "Delete inactive sessions from the database", "expected": ["db-write-protocol"] },
    { "id": "nestjs-guard", "prompt": "Add a NestJS guard and an interceptor, plus a provider built with useFactory in a dynamic module", "expected": ["nestjs-patterns"] },
    { "id": "nestjs-rules", "prompt": "Review this NestJS module for dependency injection, security, and performance issues", "expected": ["nestjs-best-practices"] },
    { "id": "clean-arch", "prompt": "Design a new domain module with presentation, application, domain, and infrastructure layers and a repository port", "expected": ["nestjs-clean-architecture"] },
    { "id": "node-defaults", "prompt": "Help me choose a Node.js framework and async patterns for a new backend service", "expected": ["nodejs-best-practices"] },
    { "id": "bug-failing-test", "prompt": "This test is failing intermittently in CI — investigate the root cause", "expected": ["bug-investigation"] },
    { "id": "plan-feat", "prompt": "Plan a refactor that splits the chat feature into three smaller modules", "expected": ["plan-mode"] },
    { "id": "adr-decision", "prompt": "We need to decide between Zustand and Jotai for the new feature; document the rationale", "expected": ["documentation-and-adrs", "decision-rules"] },
    { "id": "ts-generics", "prompt": "Define a type-safe generic hook with conditional return types based on the input shape", "expected": ["typescript-advanced-types"] },
    { "id": "async-patterns", "prompt": "Refactor this Promise.all to allow partial failures using Promise.allSettled with AbortSignal", "expected": ["async-error-handling"] },
    { "id": "complexity-nested", "prompt": "Flatten the nested conditionals in this function using guard clauses and early returns", "expected": ["cyclomatic-complexity"] },
    { "id": "perf-hotpath", "prompt": "Optimize this hot loop for large datasets and high-frequency events", "expected": ["js-performance-patterns"] },
    { "id": "refactor-cleanup", "prompt": "Simplify the code I just modified for clarity and consistency, preserving behavior", "expected": ["code-simplifier"] },
    { "id": "skill-audit", "prompt": "Review the skill library quality and check for misfiring or overlapping skills", "expected": ["meta-skill-hygiene"] },
    { "id": "git-pr", "prompt": "Commit the changes and open a pull request against main", "expected": ["git-workflow"] },
    { "id": "pushback", "prompt": "I want to introduce a new state library for this one widget — talk me out of it if that's wrong", "expected": ["pushback-templates", "decision-rules"] },
    { "id": "quality-gate", "prompt": "Set up the CI pipeline and pre-commit hooks to run typecheck, lint, and tests on every pull request", "expected": ["quality-gates"] },
    { "id": "rlm", "prompt": "I'm new to this repo — help me understand the chat feature's architecture", "expected": ["rlm-explore"] },
    { "id": "cross-repo-feat", "prompt": "Add a /users/me endpoint in the backend repo and a useMe() hook in the frontend repo using TanStack Query with caching and optimistic updates", "expected": ["cross-repo-workspace", "react-data-fetching"] },
    { "id": "spec-first", "prompt": "Add CSV export to the reports page — write the spec for it before any code", "expected": ["spec-workflow"] },
    { "id": "tailwind-theme", "prompt": "Set up dark mode theming with Tailwind v4 CSS variables for our shadcn components", "expected": ["tailwind-v4-shadcn"] }
  ]
}
